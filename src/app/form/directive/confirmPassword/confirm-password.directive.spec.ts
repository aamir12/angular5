import { ConfirmEqualValidatorDirective } from "./confirm-password.directive";

describe("ConfirmEqualValidatorDirective", () => {
  it("should create an instance", () => {
    const directive = new ConfirmEqualValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
